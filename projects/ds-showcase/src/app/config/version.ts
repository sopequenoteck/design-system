/**
 * Configuration de version du Design System
 * Mettre à jour lors de chaque release.
 */

/** Version actuelle du package @kksdev/ds-angular */
export const DS_VERSION = '1.8.2';

/** Nombre de thèmes disponibles (light, dark, custom) */
export const DS_THEMES_COUNT = 3;

/** Nombre approximatif de tests (à mettre à jour manuellement) */
export const DS_TESTS_COUNT = 2300;
