{/* Patterns.mdx */}
import { Meta } from '@storybook/blocks';

<Meta title="Guides/Patterns" />

# Patterns de composition

Ce guide documente les patterns de composition courants pour faciliter l'utilisation combinée des composants du design system.

---

## 1. Formulaire complet (input + checkbox + button)

Pattern classique de formulaire avec validation, champs multiples et soumission.

```typescript
// src/app/signup/signup.component.ts
import { Component, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DsInputField, DsCheckbox, DsButton } from 'ds-angular';
import { faUser, faEnvelope, faLock } from '@fortawesome/free-solid-svg-icons';

@Component({
  selector: 'app-signup',
  standalone: true,
  imports: [FormsModule, DsInputField, DsCheckbox, DsButton],
  template: `
    <form (ngSubmit)="handleSubmit()" class="signup-form">
      <h2>Créer un compte</h2>

      <!-- Nom d'utilisateur -->
      <ds-input-field
        name="username"
        label="Nom d'utilisateur"
        placeholder="Choisissez un nom d'utilisateur"
        [iconStart]="userIcon"
        [(ngModel)]="username"
        [error]="usernameError()"
        required>
      </ds-input-field>

      <!-- Email -->
      <ds-input-field
        name="email"
        type="email"
        label="Adresse email"
        placeholder="exemple@domaine.com"
        [iconStart]="emailIcon"
        [(ngModel)]="email"
        [error]="emailError()"
        required>
      </ds-input-field>

      <!-- Mot de passe -->
      <ds-input-field
        name="password"
        type="password"
        label="Mot de passe"
        [togglePassword]="true"
        [iconStart]="lockIcon"
        [(ngModel)]="password"
        [error]="passwordError()"
        helper="Minimum 8 caractères"
        required>
      </ds-input-field>

      <!-- Conditions d'utilisation -->
      <ds-checkbox
        name="terms"
        label="J'accepte les conditions d'utilisation"
        [(checked)]="acceptTerms"
        [error]="termsError()">
      </ds-checkbox>

      <!-- Bouton de soumission -->
      <ds-button
        variant="primary"
        submit
        block
        [loading]="isSubmitting()"
        [disabled]="!isFormValid()">
        Créer mon compte
      </ds-button>
    </form>
  `,
  styles: [`
    .signup-form {
      max-width: 400px;
      margin: 0 auto;
      padding: var(--space-6);
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
  `]
})
export class SignupComponent {
  username = signal('');
  email = signal('');
  password = signal('');
  acceptTerms = signal(false);
  isSubmitting = signal(false);

  userIcon = faUser;
  emailIcon = faEnvelope;
  lockIcon = faLock;

  usernameError() {
    const value = this.username();
    if (!value) return 'Nom d'utilisateur requis';
    if (value.length < 3) return 'Minimum 3 caractères';
    return '';
  }

  emailError() {
    const value = this.email();
    if (!value) return 'Email requis';
    if (!value.includes('@')) return 'Email invalide';
    return '';
  }

  passwordError() {
    const value = this.password();
    if (!value) return 'Mot de passe requis';
    if (value.length < 8) return 'Minimum 8 caractères';
    return '';
  }

  termsError() {
    return this.acceptTerms() ? '' : 'Vous devez accepter les conditions';
  }

  isFormValid() {
    return !this.usernameError() &&
           !this.emailError() &&
           !this.passwordError() &&
           this.acceptTerms();
  }

  handleSubmit() {
    if (!this.isFormValid()) return;

    this.isSubmitting.set(true);
    // Simulation appel API
    setTimeout(() => {
      console.log('Compte créé:', {
        username: this.username(),
        email: this.email()
      });
      this.isSubmitting.set(false);
    }, 2000);
  }
}
```

---

## 2. Modal avec formulaire

Pattern de modal contenant un formulaire de confirmation ou d'édition.

```typescript
// src/app/edit-profile/edit-profile.component.ts
import { Component, signal, viewChild } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DsModal, DsInputField, DsButton, DsInputTextarea } from 'ds-angular';

@Component({
  selector: 'app-edit-profile',
  standalone: true,
  imports: [FormsModule, DsModal, DsInputField, DsInputTextarea, DsButton],
  template: `
    <ds-button variant="primary" (clicked)="openModal()">
      Éditer le profil
    </ds-button>

    <ds-modal
      title="Modifier votre profil"
      size="md"
      [closable]="true"
      [closeOnBackdrop]="false"
      (closed)="handleModalClose()">

      <form (ngSubmit)="handleSave()" class="profile-form">
        <ds-input-field
          name="displayName"
          label="Nom d'affichage"
          [(ngModel)]="displayName"
          required>
        </ds-input-field>

        <ds-input-textarea
          name="bio"
          label="Biographie"
          placeholder="Parlez-nous de vous..."
          [rows]="4"
          [maxlength]="200"
          [(ngModel)]="bio">
        </ds-input-textarea>

        <ds-input-field
          name="location"
          label="Localisation"
          placeholder="Ville, Pays"
          [(ngModel)]="location">
        </ds-input-field>

        <div class="modal-actions">
          <ds-button
            variant="ghost"
            (clicked)="closeModal()">
            Annuler
          </ds-button>
          <ds-button
            variant="primary"
            submit
            [loading]="isSaving()">
            Enregistrer
          </ds-button>
        </div>
      </form>
    </ds-modal>
  `,
  styles: [`
    .profile-form {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
    .modal-actions {
      display: flex;
      gap: var(--space-3);
      justify-content: flex-end;
      margin-top: var(--space-4);
    }
  `]
})
export class EditProfileComponent {
  modal = viewChild.required(DsModal);

  displayName = signal('John Doe');
  bio = signal('');
  location = signal('');
  isSaving = signal(false);

  openModal() {
    this.modal().open();
  }

  closeModal() {
    this.modal().close();
  }

  handleModalClose() {
    console.log('Modal fermée');
  }

  handleSave() {
    this.isSaving.set(true);
    // Simulation sauvegarde
    setTimeout(() => {
      console.log('Profil sauvegardé:', {
        displayName: this.displayName(),
        bio: this.bio(),
        location: this.location()
      });
      this.isSaving.set(false);
      this.closeModal();
    }, 1500);
  }
}
```

---

## 3. Dropdown dans toolbar

Pattern de barre d'outils avec actions via dropdown.

```typescript
// src/app/toolbar/toolbar.component.ts
import { Component } from '@angular/core';
import { DsDropdown, DsButton, DsBadge } from 'ds-angular';
import { faEllipsisV, faEdit, faTrash, faShare, faDownload } from '@fortawesome/free-solid-svg-icons';

@Component({
  selector: 'app-toolbar',
  standalone: true,
  imports: [DsDropdown, DsButton, DsBadge],
  template: `
    <div class="toolbar">
      <div class="toolbar-left">
        <h3>Mon Document</h3>
        <ds-badge variant="info" size="sm">Brouillon</ds-badge>
      </div>

      <div class="toolbar-right">
        <ds-button variant="primary" size="sm">
          Enregistrer
        </ds-button>

        <ds-dropdown
          [items]="menuItems"
          position="bottom-end"
          (itemSelected)="handleAction($event)">
          <ds-button
            variant="ghost"
            size="sm"
            [iconStart]="menuIcon"
            dropdownTrigger>
          </ds-button>
        </ds-dropdown>
      </div>
    </div>
  `,
  styles: [`
    .toolbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: var(--space-3) var(--space-4);
      background: var(--background-secondary);
      border-bottom: 1px solid var(--border-default);
    }
    .toolbar-left {
      display: flex;
      align-items: center;
      gap: var(--space-3);
    }
    .toolbar-right {
      display: flex;
      gap: var(--space-2);
    }
  `]
})
export class ToolbarComponent {
  menuIcon = faEllipsisV;

  menuItems = [
    { id: 'edit', label: 'Éditer', icon: faEdit },
    { id: 'share', label: 'Partager', icon: faShare },
    { id: 'download', label: 'Télécharger', icon: faDownload },
    { id: 'separator', label: '', disabled: true },
    { id: 'delete', label: 'Supprimer', icon: faTrash }
  ];

  handleAction(item: any) {
    console.log('Action sélectionnée:', item.id);

    switch (item.id) {
      case 'edit':
        // Logique d'édition
        break;
      case 'share':
        // Logique de partage
        break;
      case 'download':
        // Logique de téléchargement
        break;
      case 'delete':
        // Logique de suppression
        break;
    }
  }
}
```

---

## 4. Toast notifications système

Pattern de notifications système avec différents types et gestion de la pile.

```typescript
// src/app/notifications/notifications.component.ts
import { Component, inject } from '@angular/core';
import { DsButton, DsToastService } from 'ds-angular';

@Component({
  selector: 'app-notifications',
  standalone: true,
  imports: [DsButton],
  template: `
    <div class="notification-demo">
      <h2>Système de notifications</h2>

      <div class="actions">
        <ds-button
          variant="success"
          (clicked)="showSuccess()">
          Succès
        </ds-button>

        <ds-button
          variant="warning"
          (clicked)="showWarning()">
          Avertissement
        </ds-button>

        <ds-button
          variant="error"
          (clicked)="showError()">
          Erreur
        </ds-button>

        <ds-button
          variant="info"
          (clicked)="showInfo()">
          Information
        </ds-button>

        <ds-button
          variant="ghost"
          (clicked)="showMultiple()">
          Notifications multiples
        </ds-button>
      </div>
    </div>
  `,
  styles: [`
    .notification-demo {
      padding: var(--space-6);
    }
    .actions {
      display: flex;
      gap: var(--space-3);
      flex-wrap: wrap;
      margin-top: var(--space-4);
    }
  `]
})
export class NotificationsComponent {
  private toastService = inject(DsToastService);

  showSuccess() {
    this.toastService.success('Opération réussie', 'Vos modifications ont été enregistrées');
  }

  showWarning() {
    this.toastService.warning('Attention requise', 'Certains champs nécessitent votre attention');
  }

  showError() {
    this.toastService.error('Erreur', 'Impossible de se connecter au serveur');
  }

  showInfo() {
    this.toastService.info('Information', 'Une mise à jour est disponible');
  }

  showMultiple() {
    this.toastService.success('Premier message', 'Opération 1/3');
    setTimeout(() => {
      this.toastService.info('Deuxième message', 'Opération 2/3');
    }, 500);
    setTimeout(() => {
      this.toastService.success('Terminé', 'Toutes les opérations sont complétées');
    }, 1000);
  }
}
```

**Bonnes pratiques pour les toasts** :

- Limiter la durée d'affichage (3-5 secondes pour success/info, 7-10 secondes pour warning/error)
- Éviter plus de 3 toasts simultanés
- Utiliser des messages courts et actionables
- Positionner en top-right pour ne pas bloquer le contenu principal
- Permettre la fermeture manuelle pour les messages importants

---

## 5. Carte avec Alert

Pattern de carte enrichie avec bannière d'alerte contextuelle.

```typescript
// src/app/account-card/account-card.component.ts
import { Component, signal } from '@angular/core';
import { DsCard, DsAlert, DsButton, DsBadge } from 'ds-angular';

@Component({
  selector: 'app-account-card',
  standalone: true,
  imports: [DsCard, DsAlert, DsButton, DsBadge],
  template: `
    <ds-card variant="elevated" size="lg">
      <!-- Alert intégrée en haut de la carte -->
      <ds-alert
        type="warning"
        [closable]="true"
        (closed)="handleAlertClose()"
        *ngIf="showAlert()">
        Votre abonnement expire dans 7 jours. Renouvelez-le maintenant pour éviter toute interruption.
      </ds-alert>

      <!-- En-tête de carte -->
      <div class="card-header">
        <div>
          <h3>Compte Premium</h3>
          <ds-badge variant="success" size="md">Actif</ds-badge>
        </div>
        <p class="expiry">Expire le 15/12/2025</p>
      </div>

      <!-- Contenu principal -->
      <div class="card-body">
        <dl class="details-list">
          <div class="detail-item">
            <dt>Stockage utilisé</dt>
            <dd>45 GB / 100 GB</dd>
          </div>
          <div class="detail-item">
            <dt>Utilisateurs actifs</dt>
            <dd>3 / 5</dd>
          </div>
          <div class="detail-item">
            <dt>Projets</dt>
            <dd>12</dd>
          </div>
        </dl>
      </div>

      <!-- Actions -->
      <div class="card-footer">
        <ds-button variant="outline" size="md">
          Voir les détails
        </ds-button>
        <ds-button variant="primary" size="md">
          Renouveler maintenant
        </ds-button>
      </div>
    </ds-card>
  `,
  styles: [`
    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: var(--space-4);
      padding-bottom: var(--space-4);
      border-bottom: 1px solid var(--border-subtle);
    }
    .card-header > div {
      display: flex;
      align-items: center;
      gap: var(--space-3);
    }
    .expiry {
      color: var(--text-muted);
      font-size: var(--font-size-sm);
    }
    .card-body {
      margin: var(--space-4) 0;
    }
    .details-list {
      display: grid;
      gap: var(--space-3);
    }
    .detail-item {
      display: flex;
      justify-content: space-between;
      padding: var(--space-2) 0;
    }
    .detail-item dt {
      color: var(--text-muted);
      font-weight: normal;
    }
    .detail-item dd {
      font-weight: 600;
      margin: 0;
    }
    .card-footer {
      display: flex;
      gap: var(--space-3);
      justify-content: flex-end;
      padding-top: var(--space-4);
      border-top: 1px solid var(--border-subtle);
    }
  `]
})
export class AccountCardComponent {
  showAlert = signal(true);

  handleAlertClose() {
    this.showAlert.set(false);
  }
}
```

**Variantes utiles** :

- **Carte avec alerte succès** : Pour confirmer une action récente
- **Carte avec alerte erreur** : Pour signaler un problème nécessitant attention
- **Carte avec alerte info** : Pour afficher des informations contextuelles

---

## 6. Divider dans liste

Pattern d'utilisation du divider pour séparer visuellement des groupes d'éléments.

```typescript
// src/app/settings-list/settings-list.component.ts
import { Component } from '@angular/core';
import { DsDivider, DsToggle } from 'ds-angular';
import { faUser, faBell, faLock, faGlobe, faPalette } from '@fortawesome/free-solid-svg-icons';

@Component({
  selector: 'app-settings-list',
  standalone: true,
  imports: [DsDivider, DsToggle],
  template: `
    <div class="settings-container">
      <h2>Paramètres</h2>

      <!-- Section Compte -->
      <div class="settings-section">
        <div class="setting-item">
          <fa-icon [icon]="userIcon" class="icon"></fa-icon>
          <div class="setting-content">
            <h4>Profil public</h4>
            <p>Rendre votre profil visible par tous les utilisateurs</p>
          </div>
          <ds-toggle [checked]="true"></ds-toggle>
        </div>

        <div class="setting-item">
          <fa-icon [icon]="bellIcon" class="icon"></fa-icon>
          <div class="setting-content">
            <h4>Notifications par email</h4>
            <p>Recevoir des alertes par email</p>
          </div>
          <ds-toggle [checked]="false"></ds-toggle>
        </div>
      </div>

      <!-- Divider avec label -->
      <ds-divider
        [withLabel]="true"
        labelPosition="left"
        spacing="lg">
        Sécurité
      </ds-divider>

      <!-- Section Sécurité -->
      <div class="settings-section">
        <div class="setting-item">
          <fa-icon [icon]="lockIcon" class="icon"></fa-icon>
          <div class="setting-content">
            <h4>Authentification à deux facteurs</h4>
            <p>Ajouter une couche de sécurité supplémentaire</p>
          </div>
          <ds-toggle [checked]="true"></ds-toggle>
        </div>
      </div>

      <!-- Divider simple -->
      <ds-divider variant="dashed" spacing="lg"></ds-divider>

      <!-- Section Apparence -->
      <div class="settings-section">
        <div class="setting-item">
          <fa-icon [icon]="globeIcon" class="icon"></fa-icon>
          <div class="setting-content">
            <h4>Langue</h4>
            <p>Français</p>
          </div>
        </div>

        <div class="setting-item">
          <fa-icon [icon]="paletteIcon" class="icon"></fa-icon>
          <div class="setting-content">
            <h4>Thème</h4>
            <p>Mode sombre</p>
          </div>
          <ds-toggle [checked]="true"></ds-toggle>
        </div>
      </div>
    </div>
  `,
  styles: [`
    .settings-container {
      max-width: 600px;
      margin: 0 auto;
      padding: var(--space-6);
    }
    .settings-section {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
    .setting-item {
      display: flex;
      align-items: flex-start;
      gap: var(--space-4);
      padding: var(--space-3);
      border-radius: var(--radius-2);
      transition: background-color 0.2s;
    }
    .setting-item:hover {
      background-color: var(--background-hover);
    }
    .icon {
      color: var(--text-muted);
      font-size: 1.25rem;
      margin-top: 0.125rem;
      flex-shrink: 0;
    }
    .setting-content {
      flex: 1;
    }
    .setting-content h4 {
      margin: 0 0 var(--space-1) 0;
      font-size: var(--font-size-base);
      font-weight: 600;
    }
    .setting-content p {
      margin: 0;
      color: var(--text-muted);
      font-size: var(--font-size-sm);
    }
  `]
})
export class SettingsListComponent {
  userIcon = faUser;
  bellIcon = faBell;
  lockIcon = faLock;
  globeIcon = faGlobe;
  paletteIcon = faPalette;
}
```

**Variantes de divider** :

- **`variant="solid"`** (par défaut) : Ligne continue pour séparations fortes
- **`variant="dashed"`** : Ligne pointillée pour séparations visuelles légères
- **`variant="dotted"`** : Séparation très subtile
- **`spacing`** : Contrôle l'espacement vertical (`none`, `sm`, `md`, `lg`)
- **`withLabel`** : Ajoute un label au divider pour nommer les sections

---

## 7. Responsive patterns

Le design system fournit des tokens breakpoints pour créer des layouts responsives.

### Breakpoints disponibles

<table style={{width: '100%', borderCollapse: 'collapse', marginBottom: '16px'}}>
  <thead>
    <tr style={{borderBottom: '2px solid var(--border-color, #e5e5e5)'}}>
      <th style={{textAlign: 'left', padding: '12px 16px', fontWeight: 600}}>Token</th>
      <th style={{textAlign: 'left', padding: '12px 16px', fontWeight: 600, width: '100px'}}>Valeur</th>
      <th style={{textAlign: 'left', padding: '12px 16px', fontWeight: 600}}>Usage</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{borderBottom: '1px solid var(--border-color, #e5e5e5)'}}>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-xs</code></td>
      <td style={{padding: '12px 16px'}}>320px</td>
      <td style={{padding: '12px 16px'}}>Mobile small</td>
    </tr>
    <tr style={{borderBottom: '1px solid var(--border-color, #e5e5e5)'}}>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-sm</code></td>
      <td style={{padding: '12px 16px'}}>576px</td>
      <td style={{padding: '12px 16px'}}>Mobile large</td>
    </tr>
    <tr style={{borderBottom: '1px solid var(--border-color, #e5e5e5)'}}>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-md</code></td>
      <td style={{padding: '12px 16px'}}>768px</td>
      <td style={{padding: '12px 16px'}}>Tablet</td>
    </tr>
    <tr style={{borderBottom: '1px solid var(--border-color, #e5e5e5)'}}>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-lg</code></td>
      <td style={{padding: '12px 16px'}}>992px</td>
      <td style={{padding: '12px 16px'}}>Desktop</td>
    </tr>
    <tr style={{borderBottom: '1px solid var(--border-color, #e5e5e5)'}}>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-xl</code></td>
      <td style={{padding: '12px 16px'}}>1200px</td>
      <td style={{padding: '12px 16px'}}>Large desktop</td>
    </tr>
    <tr>
      <td style={{padding: '12px 16px'}}><code>--breakpoint-2xl</code></td>
      <td style={{padding: '12px 16px'}}>1400px</td>
      <td style={{padding: '12px 16px'}}>Extra large</td>
    </tr>
  </tbody>
</table>

### Exemple 1 : Grid responsive

```typescript
@Component({
  selector: 'app-responsive-grid',
  standalone: true,
  imports: [DsCard],
  template: `
    <div class="grid">
      @for (item of items; track item.id) {
        <ds-card variant="outlined">
          <ng-container card-header>{{ item.title }}</ng-container>
          <ng-container card-body>{{ item.description }}</ng-container>
        </ds-card>
      }
    </div>
  `,
  styles: [`
    .grid {
      display: grid;
      gap: var(--space-4);
      grid-template-columns: 1fr;
    }

    /* Tablet : 2 colonnes */
    @media (min-width: 768px) {
      .grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    /* Desktop : 3 colonnes */
    @media (min-width: 992px) {
      .grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    /* Large : 4 colonnes */
    @media (min-width: 1200px) {
      .grid {
        grid-template-columns: repeat(4, 1fr);
      }
    }
  `]
})
export class ResponsiveGridComponent {
  items = [
    { id: 1, title: 'Item 1', description: 'Description 1' },
    { id: 2, title: 'Item 2', description: 'Description 2' },
    { id: 3, title: 'Item 3', description: 'Description 3' },
    { id: 4, title: 'Item 4', description: 'Description 4' },
  ];
}
```

### Exemple 2 : Navigation adaptive

```typescript
@Component({
  selector: 'app-adaptive-nav',
  standalone: true,
  imports: [DsTabs, DsDropdown],
  template: `
    <!-- Navigation desktop : Tabs -->
    <div class="nav-desktop">
      <ds-tabs [tabs]="navItems" (tabChange)="onNavigate($event)"></ds-tabs>
    </div>

    <!-- Navigation mobile : Dropdown -->
    <div class="nav-mobile">
      <ds-dropdown
        [items]="dropdownItems"
        [selectedValue]="selectedNav"
        (selectionChange)="onNavigate($event)">
      </ds-dropdown>
    </div>
  `,
  styles: [`
    .nav-desktop {
      display: none;
    }
    .nav-mobile {
      display: block;
    }

    /* Tablet et plus : afficher tabs */
    @media (min-width: 768px) {
      .nav-desktop {
        display: block;
      }
      .nav-mobile {
        display: none;
      }
    }
  `]
})
export class AdaptiveNavComponent {
  navItems = [
    { id: 'home', label: 'Accueil' },
    { id: 'products', label: 'Produits' },
    { id: 'contact', label: 'Contact' },
  ];

  dropdownItems = this.navItems.map(item => ({
    id: item.id,
    label: item.label,
    value: item.id,
  }));

  selectedNav = 'home';

  onNavigate(item: any) {
    this.selectedNav = item.id || item;
    console.log('Navigate to:', this.selectedNav);
  }
}
```

### Exemple 3 : Container queries

Les Container Queries permettent des layouts réactifs basés sur la taille du conteneur.

```typescript
@Component({
  selector: 'app-container-query',
  standalone: true,
  imports: [DsCard, DsButton],
  template: `
    <div class="card-container">
      <ds-card>
        <ng-container card-body>
          <div class="card-layout">
            <div class="card-image">
              <div class="placeholder"></div>
            </div>
            <div class="card-content">
              <h3>Titre du produit</h3>
              <p>Description du produit avec détails.</p>
              <ds-button variant="primary">Acheter</ds-button>
            </div>
          </div>
        </ng-container>
      </ds-card>
    </div>
  `,
  styles: [`
    .card-container {
      container-type: inline-size;
      container-name: card;
    }

    .card-layout {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }

    .card-image .placeholder {
      background: var(--background-secondary);
      aspect-ratio: 16/9;
      border-radius: var(--radius-2);
    }

    /* Layout horizontal si conteneur > 400px */
    @container card (min-width: 400px) {
      .card-layout {
        flex-direction: row;
      }
      .card-image {
        width: 40%;
        flex-shrink: 0;
      }
      .card-content {
        flex: 1;
      }
    }
  `]
})
export class ContainerQueryComponent {}
```

### Bonnes pratiques responsive

1. **Mobile-first** : Commencer par les styles mobile, ajouter les breakpoints pour les écrans plus grands
2. **Tokens** : Utiliser les tokens CSS custom properties pour la cohérence
3. **Container queries** : Préférer aux media queries pour les composants réutilisables
4. **Flexbox/Grid** : Utiliser CSS Grid pour les layouts 2D, Flexbox pour les layouts 1D
5. **Tailles relatives** : Utiliser `rem` et `%` plutôt que `px` pour les tailles

---

## 8. Wizard multi-étapes

Pattern de formulaire multi-étapes avec stepper, validation et navigation.

```typescript
// src/app/checkout-wizard/checkout-wizard.component.ts
import { Component, signal, computed } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DsStepper, DsCard, DsInputField, DsButton, DsRadioGroup, DsAlert } from 'ds-angular';
import { faUser, faShoppingCart, faCreditCard, faCheck } from '@fortawesome/free-solid-svg-icons';

interface WizardStep {
  id: string;
  label: string;
  description?: string;
  icon?: any;
  optional?: boolean;
}

@Component({
  selector: 'app-checkout-wizard',
  standalone: true,
  imports: [FormsModule, DsStepper, DsCard, DsInputField, DsButton, DsRadioGroup, DsAlert],
  template: `
    <div class="wizard-container">
      <ds-stepper
        [steps]="steps"
        [currentStep]="currentStep()"
        [linear]="true"
        orientation="horizontal"
        size="md"
        (stepChange)="onStepChange($event)">
      </ds-stepper>

      <ds-card variant="elevated" size="lg" class="wizard-content">
        <!-- Étape 1 : Informations personnelles -->
        @if (currentStep() === 0) {
          <div class="step-content">
            <h3>Informations personnelles</h3>
            <ds-input-field
              name="fullName"
              label="Nom complet"
              placeholder="Jean Dupont"
              [(ngModel)]="formData.fullName"
              [error]="errors().fullName"
              required>
            </ds-input-field>
            <ds-input-field
              name="email"
              label="Email"
              type="email"
              placeholder="jean@exemple.com"
              [(ngModel)]="formData.email"
              [error]="errors().email"
              required>
            </ds-input-field>
            <ds-input-field
              name="phone"
              label="Téléphone"
              type="tel"
              placeholder="+33 6 12 34 56 78"
              [(ngModel)]="formData.phone">
            </ds-input-field>
          </div>
        }

        <!-- Étape 2 : Livraison -->
        @if (currentStep() === 1) {
          <div class="step-content">
            <h3>Adresse de livraison</h3>
            <ds-input-field
              name="address"
              label="Adresse"
              placeholder="123 rue de la Paix"
              [(ngModel)]="formData.address"
              [error]="errors().address"
              required>
            </ds-input-field>
            <div class="row">
              <ds-input-field
                name="zipCode"
                label="Code postal"
                placeholder="75001"
                [(ngModel)]="formData.zipCode"
                required>
              </ds-input-field>
              <ds-input-field
                name="city"
                label="Ville"
                placeholder="Paris"
                [(ngModel)]="formData.city"
                required>
              </ds-input-field>
            </div>
            <ds-radio-group
              name="deliveryOption"
              label="Mode de livraison"
              [options]="deliveryOptions"
              [(ngModel)]="formData.deliveryOption">
            </ds-radio-group>
          </div>
        }

        <!-- Étape 3 : Paiement -->
        @if (currentStep() === 2) {
          <div class="step-content">
            <h3>Paiement</h3>
            <ds-alert type="info" [closable]="false">
              Vos données bancaires sont sécurisées par chiffrement SSL.
            </ds-alert>
            <ds-input-field
              name="cardNumber"
              label="Numéro de carte"
              placeholder="4242 4242 4242 4242"
              [(ngModel)]="formData.cardNumber"
              [error]="errors().cardNumber"
              required>
            </ds-input-field>
            <div class="row">
              <ds-input-field
                name="expiry"
                label="Date d'expiration"
                placeholder="MM/AA"
                [(ngModel)]="formData.expiry"
                required>
              </ds-input-field>
              <ds-input-field
                name="cvv"
                label="CVV"
                placeholder="123"
                [(ngModel)]="formData.cvv"
                required>
              </ds-input-field>
            </div>
          </div>
        }

        <!-- Étape 4 : Confirmation -->
        @if (currentStep() === 3) {
          <div class="step-content">
            <ds-alert type="success" [closable]="false">
              Commande validée avec succès !
            </ds-alert>
            <h3>Récapitulatif</h3>
            <dl class="summary">
              <div class="summary-item">
                <dt>Nom</dt>
                <dd>{{ formData.fullName }}</dd>
              </div>
              <div class="summary-item">
                <dt>Email</dt>
                <dd>{{ formData.email }}</dd>
              </div>
              <div class="summary-item">
                <dt>Adresse</dt>
                <dd>{{ formData.address }}, {{ formData.zipCode }} {{ formData.city }}</dd>
              </div>
              <div class="summary-item">
                <dt>Livraison</dt>
                <dd>{{ getDeliveryLabel() }}</dd>
              </div>
            </dl>
          </div>
        }

        <!-- Navigation -->
        <div class="wizard-actions">
          <ds-button
            variant="ghost"
            [disabled]="currentStep() === 0"
            (clicked)="previousStep()">
            Précédent
          </ds-button>
          <ds-button
            variant="primary"
            [loading]="isProcessing()"
            (clicked)="nextStep()">
            {{ currentStep() === steps.length - 1 ? 'Terminer' : 'Suivant' }}
          </ds-button>
        </div>
      </ds-card>
    </div>
  `,
  styles: [`
    .wizard-container {
      max-width: 700px;
      margin: 0 auto;
      padding: var(--space-6);
    }
    .wizard-content {
      margin-top: var(--space-6);
    }
    .step-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
      min-height: 300px;
    }
    .row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-4);
    }
    .wizard-actions {
      display: flex;
      justify-content: space-between;
      margin-top: var(--space-6);
      padding-top: var(--space-4);
      border-top: 1px solid var(--border-color);
    }
    .summary {
      background: var(--background-secondary);
      padding: var(--space-4);
      border-radius: var(--radius-2);
    }
    .summary-item {
      display: flex;
      justify-content: space-between;
      padding: var(--space-2) 0;
      border-bottom: 1px solid var(--border-color);
    }
    .summary-item:last-child {
      border-bottom: none;
    }
    .summary-item dt {
      color: var(--text-muted);
    }
    .summary-item dd {
      font-weight: 600;
      margin: 0;
    }
  `]
})
export class CheckoutWizardComponent {
  steps: WizardStep[] = [
    { id: 'info', label: 'Informations', description: 'Vos coordonnées', icon: faUser },
    { id: 'shipping', label: 'Livraison', description: 'Adresse et mode', icon: faShoppingCart },
    { id: 'payment', label: 'Paiement', description: 'Carte bancaire', icon: faCreditCard },
    { id: 'confirm', label: 'Confirmation', description: 'Récapitulatif', icon: faCheck },
  ];

  currentStep = signal(0);
  isProcessing = signal(false);

  formData = {
    fullName: '',
    email: '',
    phone: '',
    address: '',
    zipCode: '',
    city: '',
    deliveryOption: 'standard',
    cardNumber: '',
    expiry: '',
    cvv: '',
  };

  deliveryOptions = [
    { value: 'standard', label: 'Standard (3-5 jours)', description: 'Gratuit' },
    { value: 'express', label: 'Express (1-2 jours)', description: '9,99 €' },
    { value: 'premium', label: 'Premium (24h)', description: '19,99 €' },
  ];

  errors = computed(() => ({
    fullName: !this.formData.fullName ? 'Nom requis' : '',
    email: !this.formData.email ? 'Email requis' : '',
    address: !this.formData.address ? 'Adresse requise' : '',
    cardNumber: !this.formData.cardNumber ? 'Numéro de carte requis' : '',
  }));

  onStepChange(step: number) {
    if (step < this.currentStep()) {
      this.currentStep.set(step);
    }
  }

  previousStep() {
    if (this.currentStep() > 0) {
      this.currentStep.update(s => s - 1);
    }
  }

  nextStep() {
    if (this.currentStep() < this.steps.length - 1) {
      this.isProcessing.set(true);
      setTimeout(() => {
        this.currentStep.update(s => s + 1);
        this.isProcessing.set(false);
      }, 500);
    }
  }

  getDeliveryLabel() {
    return this.deliveryOptions.find(o => o.value === this.formData.deliveryOption)?.label || '';
  }
}
```

**Bonnes pratiques Wizard** :

- Limiter à 4-5 étapes maximum
- Afficher la progression clairement avec le stepper
- Permettre la navigation arrière sans perte de données
- Valider chaque étape avant de passer à la suivante
- Afficher un récapitulatif final avant confirmation

---

## 9. Liste paginée

Pattern de liste avec pagination, état de chargement et gestion des données.

```typescript
// src/app/user-list/user-list.component.ts
import { Component, signal, computed, effect } from '@angular/core';
import { DsPagination, DsCard, DsSkeleton, DsBadge, DsInputField } from 'ds-angular';
import { faSearch, faUser } from '@fortawesome/free-solid-svg-icons';

interface User {
  id: number;
  name: string;
  email: string;
  role: 'admin' | 'user' | 'guest';
  status: 'active' | 'inactive';
}

@Component({
  selector: 'app-user-list',
  standalone: true,
  imports: [DsPagination, DsCard, DsSkeleton, DsBadge, DsInputField],
  template: `
    <div class="list-container">
      <div class="list-header">
        <h2>Utilisateurs</h2>
        <ds-input-field
          name="search"
          placeholder="Rechercher..."
          [iconStart]="searchIcon"
          [(ngModel)]="searchQuery"
          size="sm">
        </ds-input-field>
      </div>

      <!-- État de chargement avec Skeleton -->
      @if (isLoading()) {
        <div class="skeleton-list">
          @for (item of [1,2,3,4,5]; track item) {
            <ds-card variant="outlined">
              <ng-container card-body>
                <div class="user-skeleton">
                  <ds-skeleton variant="circle" width="48px" height="48px"></ds-skeleton>
                  <div class="user-skeleton-content">
                    <ds-skeleton variant="text" width="60%"></ds-skeleton>
                    <ds-skeleton variant="text" width="40%" height="14px"></ds-skeleton>
                  </div>
                  <ds-skeleton variant="rectangle" width="80px" height="24px"></ds-skeleton>
                </div>
              </ng-container>
            </ds-card>
          }
        </div>
      }

      <!-- Liste des utilisateurs -->
      @if (!isLoading()) {
        <div class="user-list">
          @for (user of paginatedUsers(); track user.id) {
            <ds-card variant="outlined" [clickable]="true" (click)="selectUser(user)">
              <ng-container card-body>
                <div class="user-item">
                  <div class="user-avatar">
                    <fa-icon [icon]="userIcon"></fa-icon>
                  </div>
                  <div class="user-info">
                    <h4>{{ user.name }}</h4>
                    <p>{{ user.email }}</p>
                  </div>
                  <div class="user-meta">
                    <ds-badge
                      [variant]="user.status === 'active' ? 'success' : 'secondary'"
                      size="sm">
                      {{ user.status === 'active' ? 'Actif' : 'Inactif' }}
                    </ds-badge>
                    <ds-badge
                      [variant]="getRoleBadgeVariant(user.role)"
                      size="sm">
                      {{ getRoleLabel(user.role) }}
                    </ds-badge>
                  </div>
                </div>
              </ng-container>
            </ds-card>
          } @empty {
            <ds-card variant="outlined">
              <ng-container card-body>
                <p class="empty-state">Aucun utilisateur trouvé</p>
              </ng-container>
            </ds-card>
          }
        </div>

        <!-- Pagination -->
        <ds-pagination
          [totalItems]="filteredUsers().length"
          [pageSize]="pageSize()"
          [currentPage]="currentPage()"
          [pageSizeOptions]="[5, 10, 20, 50]"
          [showPageSizeSelector]="true"
          [showTotalItems]="true"
          [showFirstLast]="true"
          size="md"
          (pageChange)="onPageChange($event)"
          (pageSizeChange)="onPageSizeChange($event)">
        </ds-pagination>
      }
    </div>
  `,
  styles: [`
    .list-container {
      max-width: 800px;
      margin: 0 auto;
      padding: var(--space-6);
    }
    .list-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--space-4);
    }
    .list-header h2 {
      margin: 0;
    }
    .user-list, .skeleton-list {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      margin-bottom: var(--space-4);
    }
    .user-item, .user-skeleton {
      display: flex;
      align-items: center;
      gap: var(--space-4);
    }
    .user-avatar {
      width: 48px;
      height: 48px;
      background: var(--background-secondary);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
      font-size: 1.25rem;
      flex-shrink: 0;
    }
    .user-info {
      flex: 1;
      min-width: 0;
    }
    .user-info h4 {
      margin: 0 0 var(--space-1) 0;
      font-size: var(--font-size-base);
    }
    .user-info p {
      margin: 0;
      color: var(--text-muted);
      font-size: var(--font-size-sm);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .user-meta {
      display: flex;
      gap: var(--space-2);
      flex-shrink: 0;
    }
    .user-skeleton-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: var(--space-2);
    }
    .empty-state {
      text-align: center;
      color: var(--text-muted);
      padding: var(--space-6);
    }
  `]
})
export class UserListComponent {
  searchIcon = faSearch;
  userIcon = faUser;

  // Données simulées
  allUsers: User[] = Array.from({ length: 47 }, (_, i) => ({
    id: i + 1,
    name: `Utilisateur ${i + 1}`,
    email: `user${i + 1}@example.com`,
    role: ['admin', 'user', 'guest'][i % 3] as User['role'],
    status: i % 4 === 0 ? 'inactive' : 'active' as User['status'],
  }));

  isLoading = signal(true);
  searchQuery = signal('');
  currentPage = signal(1);
  pageSize = signal(10);

  filteredUsers = computed(() => {
    const query = this.searchQuery().toLowerCase();
    if (!query) return this.allUsers;
    return this.allUsers.filter(u =>
      u.name.toLowerCase().includes(query) ||
      u.email.toLowerCase().includes(query)
    );
  });

  paginatedUsers = computed(() => {
    const start = (this.currentPage() - 1) * this.pageSize();
    const end = start + this.pageSize();
    return this.filteredUsers().slice(start, end);
  });

  constructor() {
    // Simuler le chargement initial
    setTimeout(() => this.isLoading.set(false), 1500);

    // Reset page quand la recherche change
    effect(() => {
      this.searchQuery();
      this.currentPage.set(1);
    });
  }

  onPageChange(page: number) {
    this.currentPage.set(page);
  }

  onPageSizeChange(size: number) {
    this.pageSize.set(size);
    this.currentPage.set(1);
  }

  selectUser(user: User) {
    console.log('User selected:', user);
  }

  getRoleBadgeVariant(role: string) {
    switch (role) {
      case 'admin': return 'error';
      case 'user': return 'info';
      default: return 'secondary';
    }
  }

  getRoleLabel(role: string) {
    switch (role) {
      case 'admin': return 'Admin';
      case 'user': return 'Utilisateur';
      default: return 'Invité';
    }
  }
}
```

**Bonnes pratiques Liste paginée** :

- Afficher un skeleton pendant le chargement
- Permettre le choix de la taille de page
- Afficher le nombre total d'éléments
- Reset la pagination lors d'un changement de filtre
- Gérer l'état vide avec un message explicite
- Optimiser les performances avec `track` dans les boucles

---

## 10. Formulaire de filtrage avec DatePicker et SearchInput

Exemple complet d'un formulaire de filtrage combinant recherche textuelle et sélection de dates.

```typescript
import { Component, signal, computed, effect } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule, ReactiveFormsModule, FormGroup, FormControl } from '@angular/forms';
import { DsSearchInput } from 'ds-angular/components/ds-search-input';
import { DsDatePicker } from 'ds-angular/components/ds-date-picker';
import { DsButton } from 'ds-angular/components/ds-button';
import { DsCard } from 'ds-angular/components/ds-card';
import { DsBadge } from 'ds-angular/components/ds-badge';
import { DsToast, DsToastService } from 'ds-angular/components/ds-toast';
import { DsTable } from 'ds-angular/components/ds-table';

interface Event {
  id: number;
  title: string;
  date: Date;
  category: string;
  status: 'upcoming' | 'ongoing' | 'completed';
}

@Component({
  selector: 'app-event-filter',
  standalone: true,
  imports: [
    CommonModule,
    FormsModule,
    ReactiveFormsModule,
    DsSearchInput,
    DsDatePicker,
    DsButton,
    DsCard,
    DsBadge,
    DsToast,
    DsTable,
  ],
  template: `
    <div class="filter-container">
      <ds-card variant="elevated">
        <ng-container dsCardHeader>
          <h3>Filtrer les événements</h3>
        </ng-container>

        <ng-container dsCardBody>
          <form [formGroup]="filterForm" class="filter-form">
            <!-- Recherche textuelle -->
            <div class="filter-field">
              <label>Rechercher un événement</label>
              <ds-search-input
                formControlName="search"
                placeholder="Titre, catégorie..."
                size="md"
                [debounceTime]="300"
                (searchChange)="onSearchChange($event)"
              />
            </div>

            <!-- Sélection de dates -->
            <div class="filter-dates">
              <div class="filter-field">
                <label>Date de début</label>
                <ds-date-picker
                  formControlName="startDate"
                  size="md"
                  [minDate]="minDate"
                  placeholder="Sélectionner..."
                />
              </div>

              <div class="filter-field">
                <label>Date de fin</label>
                <ds-date-picker
                  formControlName="endDate"
                  size="md"
                  [minDate]="filterForm.get('startDate')?.value"
                  placeholder="Sélectionner..."
                />
              </div>
            </div>

            <!-- Actions -->
            <div class="filter-actions">
              <ds-button
                variant="secondary"
                (click)="resetFilters()"
                [disabled]="!hasActiveFilters()"
              >
                Réinitialiser
              </ds-button>
              <ds-button
                variant="primary"
                (click)="applyFilters()"
              >
                Appliquer ({{ filteredCount() }} résultats)
              </ds-button>
            </div>
          </form>
        </ng-container>
      </ds-card>

      <!-- Filtres actifs -->
      @if (hasActiveFilters()) {
        <div class="active-filters">
          <span class="active-filters-label">Filtres actifs :</span>
          @if (filterForm.get('search')?.value) {
            <ds-badge variant="info" size="sm">
              Recherche : {{ filterForm.get('search')?.value }}
              <button class="badge-close" (click)="clearSearch()">×</button>
            </ds-badge>
          }
          @if (filterForm.get('startDate')?.value) {
            <ds-badge variant="info" size="sm">
              Depuis : {{ filterForm.get('startDate')?.value | date:'dd/MM/yyyy' }}
              <button class="badge-close" (click)="clearStartDate()">×</button>
            </ds-badge>
          }
          @if (filterForm.get('endDate')?.value) {
            <ds-badge variant="info" size="sm">
              Jusqu'au : {{ filterForm.get('endDate')?.value | date:'dd/MM/yyyy' }}
              <button class="badge-close" (click)="clearEndDate()">×</button>
            </ds-badge>
          }
        </div>
      }

      <!-- Résultats -->
      <ds-card class="results-card">
        <ng-container dsCardBody>
          <ds-table
            [data]="filteredEvents()"
            [columns]="columns"
            [striped]="true"
            [hoverable]="true"
            emptyMessage="Aucun événement ne correspond à vos critères"
          >
            <ng-template #cellTemplate let-row let-column="column">
              @switch (column.key) {
                @case ('date') {
                  {{ row.date | date:'dd MMM yyyy' }}
                }
                @case ('status') {
                  <ds-badge [variant]="getStatusVariant(row.status)" size="sm">
                    {{ getStatusLabel(row.status) }}
                  </ds-badge>
                }
                @default {
                  {{ row[column.key] }}
                }
              }
            </ng-template>
          </ds-table>
        </ng-container>
      </ds-card>
    </div>

    <ds-toast />
  `,
  styles: [`
    .filter-container {
      max-width: 900px;
      margin: 0 auto;
      padding: var(--space-6);
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
    .filter-form {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
    .filter-field {
      display: flex;
      flex-direction: column;
      gap: var(--space-2);
    }
    .filter-field label {
      font-weight: 500;
      font-size: var(--font-size-sm);
      color: var(--text-muted);
    }
    .filter-dates {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-4);
    }
    .filter-actions {
      display: flex;
      justify-content: flex-end;
      gap: var(--space-3);
      padding-top: var(--space-2);
    }
    .active-filters {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: var(--space-2);
    }
    .active-filters-label {
      font-size: var(--font-size-sm);
      color: var(--text-muted);
    }
    .badge-close {
      margin-left: var(--space-1);
      background: none;
      border: none;
      cursor: pointer;
      color: inherit;
      font-size: 1.1em;
      opacity: 0.7;
    }
    .badge-close:hover {
      opacity: 1;
    }
    .results-card {
      margin-top: var(--space-2);
    }
    @media (max-width: 600px) {
      .filter-dates {
        grid-template-columns: 1fr;
      }
    }
  `]
})
export class EventFilterComponent {
  minDate = new Date();

  filterForm = new FormGroup({
    search: new FormControl(''),
    startDate: new FormControl<Date | null>(null),
    endDate: new FormControl<Date | null>(null),
  });

  columns = [
    { key: 'title', label: 'Titre', sortable: true },
    { key: 'category', label: 'Catégorie', sortable: true },
    { key: 'date', label: 'Date', sortable: true },
    { key: 'status', label: 'Statut' },
  ];

  // Données simulées
  allEvents: Event[] = [
    { id: 1, title: 'Conférence Angular', date: new Date('2025-01-15'), category: 'Tech', status: 'upcoming' },
    { id: 2, title: 'Workshop Design System', date: new Date('2025-01-20'), category: 'Design', status: 'upcoming' },
    { id: 3, title: 'Meetup TypeScript', date: new Date('2025-02-01'), category: 'Tech', status: 'upcoming' },
    { id: 4, title: 'Formation A11y', date: new Date('2024-12-01'), category: 'UX', status: 'completed' },
    { id: 5, title: 'Hackathon Innovation', date: new Date('2025-01-10'), category: 'Tech', status: 'ongoing' },
  ];

  filteredEvents = signal<Event[]>(this.allEvents);
  filteredCount = computed(() => this.filteredEvents().length);

  constructor(private toastService: DsToastService) {
    // Filtrage réactif
    effect(() => {
      this.applyFiltersInternal();
    });
  }

  hasActiveFilters(): boolean {
    const { search, startDate, endDate } = this.filterForm.value;
    return !!(search || startDate || endDate);
  }

  onSearchChange(value: string) {
    this.applyFiltersInternal();
  }

  applyFilters() {
    this.applyFiltersInternal();
    this.toastService.show({
      type: 'success',
      message: `${this.filteredCount()} événements trouvés`,
      duration: 3000,
    });
  }

  private applyFiltersInternal() {
    const { search, startDate, endDate } = this.filterForm.value;
    let results = [...this.allEvents];

    if (search) {
      const query = search.toLowerCase();
      results = results.filter(e =>
        e.title.toLowerCase().includes(query) ||
        e.category.toLowerCase().includes(query)
      );
    }

    if (startDate) {
      results = results.filter(e => e.date >= startDate);
    }

    if (endDate) {
      results = results.filter(e => e.date <= endDate);
    }

    this.filteredEvents.set(results);
  }

  resetFilters() {
    this.filterForm.reset();
    this.filteredEvents.set(this.allEvents);
    this.toastService.show({
      type: 'info',
      message: 'Filtres réinitialisés',
      duration: 2000,
    });
  }

  clearSearch() {
    this.filterForm.patchValue({ search: '' });
  }

  clearStartDate() {
    this.filterForm.patchValue({ startDate: null });
  }

  clearEndDate() {
    this.filterForm.patchValue({ endDate: null });
  }

  getStatusVariant(status: string): string {
    switch (status) {
      case 'upcoming': return 'info';
      case 'ongoing': return 'warning';
      case 'completed': return 'success';
      default: return 'secondary';
    }
  }

  getStatusLabel(status: string): string {
    switch (status) {
      case 'upcoming': return 'À venir';
      case 'ongoing': return 'En cours';
      case 'completed': return 'Terminé';
      default: return status;
    }
  }
}
```

**Bonnes pratiques Formulaire de filtrage** :

- Utiliser `debounceTime` sur la recherche pour éviter les requêtes excessives
- Afficher le nombre de résultats avant d'appliquer les filtres
- Permettre la suppression individuelle de chaque filtre actif
- Valider que la date de fin est postérieure à la date de début
- Gérer l'état responsive avec des media queries
- Afficher un feedback utilisateur via toast après application des filtres

---

## Ressources complémentaires

- [Introduction](./Introduction) — Installation et Quick Start
- [Integration](./Integration) — Exemples d'intégration Angular avancés
- [Contributing](./Contributing) — Guide de contribution
